Scripts for HPC environment mostly. Some R, some python, some bash scripts.

## PLacing these lines here for later. 
git add ebseq/
git commit ebseq/ -m "Adding code to run ebSeq-HMM and a starting gene count file"
git push

